# Futures Total API

Canonical home for Futures Total API modules.

This mirrors the public HTTP endpoints exposed via the `ThorTrading.api` namespace,
which remains in place for Django import stability. The modules there are shims
that forward to this folder.
