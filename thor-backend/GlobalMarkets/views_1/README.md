# GlobalMarkets Views

- `viewsets.py` — DRF viewsets for markets, sessions, and configuration endpoints consumed by frontend and jobs.
- `composite.py` — Aggregated status endpoints that pull market clock/composite service data.
