# Docker/nginx: proxy through host 8001
VITE_API_BASE_URL=/api
PROXY_TARGET=http://localhost:8001

# WebSockets are OFF in prod builds unless explicitly enabled.
# Nginx proxies /ws/ to the backend ASGI service.
VITE_WS_ENABLED=1
