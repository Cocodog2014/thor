worker_processes auto;

events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;
    log_format main '$remote_addr - $remote_user [$time_local] '
                    '"$request" $status $body_bytes_sent '
                    '"$http_referer" "$http_user_agent"';

    access_log /var/log/nginx/access.log main;
    error_log  /var/log/nginx/error.log warn;

    sendfile on;
    keepalive_timeout 65;

    # Prefer any upstream X-Forwarded-Proto (e.g., from cloudflared) and
    # fall back to the immediate connection scheme.
    map $http_x_forwarded_proto $thor_x_forwarded_proto {
        default $http_x_forwarded_proto;
        ''      $scheme;
    }

    upstream thor_web {
        server web:8000;
    }

    upstream thor_asgi {
        server asgi:8001;
    }

    server {
        listen 80;
        server_name _;

        # WebSockets (Daphne / ASGI)
        location /ws/ {
            proxy_pass http://thor_asgi;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "Upgrade";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $thor_x_forwarded_proto;
        }

        location /static/ {
            alias /app/thor-backend/staticfiles/;
            access_log off;
            add_header Cache-Control "public, max-age=31536000, immutable";
            try_files $uri =404;
        }

        location /media/ {
            alias /app/thor-backend/media/;
            try_files $uri =404;
        }

        location / {
            proxy_pass         http://thor_web;
            proxy_set_header   Host $host;
            proxy_set_header   X-Real-IP $remote_addr;
            proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $thor_x_forwarded_proto;
        }

        location /nginx-health {
            return 200 "OK\n";
            add_header Content-Type text/plain;
        }
    }
}
