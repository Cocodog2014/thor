###############################################################
#  Thor Frontend Environment Variables
#  This file controls which backend the React/Vite frontend uses.
#
#  IMPORTANT:
#  - Vite ONLY reads this file at startup.
#  - If you change anything here, you MUST restart:
#        npm run dev
###############################################################


###############################################################
# BACKEND API LOCATION (Docker Gunicorn version)
#
# Use this when running:
#     docker compose up -d
#
# The Django/Gunicorn backend inside Docker listens on:
#     http://localhost:8001
#
# Your API routes live under /api, so the full Base URL becomes:
#     http://localhost:8001/api
#
# This is the correct setting for DOCKER MODE.
###############################################################
#VITE_API_BASE_URL=http://localhost:8001/api



###############################################################
# BACKEND API LOCATION (Local Django runserver version)
#
# Use this ONLY when running:
#     python manage.py runserver 0.0.0.0:8000
#
# In that case, uncomment this line and comment out the Docker one.
# DO NOT USE BOTH AT THE SAME TIME.
#
# Example:
#     # VITE_API_BASE_URL=http://localhost:8001/api   <-- disable this
#     VITE_API_BASE_URL=http://localhost:8000/api     <-- enable this
#
# For now, we leave it commented out so Docker mode works correctly.
###############################################################
# VITE_API_BASE_URL=http://localhost:8000/api

http://localhost:8001/api
#http://localhost:8000/api
